Mehr als eine Pipe pro Zeile?
Ja, man kann beliebig viele Pipes hintereinander schalten (sogenannte Pipe-Chains).

Beispiel: Wir möchten die Datei namen.txt ausgeben, nur Zeilen mit "A" finden, diese sortieren und das Ergebnis anzeigen:
cat namen.txt | grep "A" | sort